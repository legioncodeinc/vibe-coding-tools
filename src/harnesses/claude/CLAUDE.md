@AGENTS.md

## Claude Code

The shared project briefing lives in `AGENTS.md` and is imported above. Everything in it applies here.

Edit portable components under `src/`. Keep harness-specific entry templates and manifests under `src/harnesses/`. Generated adapters are local outputs and must remain ignored and untracked.
