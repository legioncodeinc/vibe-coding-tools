---
description: Placeholder Wasp Nest command shipped inside the reference plugin skeleton, showing where a plugin-bundled command lives.
---

<!-- Shipped as a commands/ flat file, not a skills/ directory, for reliable slash invocation in Cowork (see #46079 in harness-specific-reference.md). -->

Load pest-controller-suit now, before doing anything else in this command.

This is a stub. It exists to show a plugin-bundled command's file location (`commands/<name>.md`, flat markdown, at the plugin root) alongside the preferred `skills/` directory in this same reference plugin. Do not build a real command from this file directly. The real starting point is the full Wasp Nest command template at `../../../commands/reference-commands.md`, which carries the argument-placeholder convention and the close-out block a real Wasp Nest command needs.
